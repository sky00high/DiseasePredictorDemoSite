$(document).ready(function(){

	/*
	 beginAddress = localStorage.getItem('beginAddress');
	 endAddress = localStorage.getItem('endAddress');
	 loadStoredSessionNamesIntoList();
	if(beginAddress && endAddress){
		$('#restoreSessionPanel').removeClass('hidden');
		$('#lastSessionStartAddress').text(beginAddress);
		$('#lastSessionDestination').text(endAddress);
		$('#restoreSessionButton').click(function(){
			localStorage.setItem('sessionType', 'restore');
			window.location = "main.html";
		});
	}

	$('#startButton').click(function(){
		var beginAddress = $('#startAddressInput').val();
		var endAddress = $('#destinationInput').val();
		if(!beginAddress.length || !endAddress.length){
			alert('Please input addresses');
		} else {
			localStorage.setItem('beginAddress', beginAddress);
			localStorage.setItem('endAddress', endAddress);
			localStorage.setItem('transportMode', transportMode)
			localStorage.setItem('sessionType', 'new');
			window.location = "main.html";
		}
	});
	document.getElementById('driving').checked= true;// Get this from load data
	$('#driving').click(function() {
        if (document.getElementById('driving').checked == true)
        {
            transportMode = 'DRIVING';
        }
    });
    $('#walking').click(function() {
         if (document.getElementById('walking').checked == true)
        {
            transportMode = 'WALKING';
        }
    });
    $('#bicycling').click(function() {
         if (document.getElementById('bicycling').checked == true)
        {
            transportMode = 'BICYCLING';
        }
    });
	$('#loadSessionDropdownButton').click(loadStoredSessionNamesIntoList);
	*/


});

